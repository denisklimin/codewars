// This code does not execute properly. Try to figure out why.

function multiply(a, b){
    let c = 0;
    c = a * b;
    return c;
}

  // const num1 = +promt('First');
  // const num2 = +promt('Second');

multiply(1, 1);